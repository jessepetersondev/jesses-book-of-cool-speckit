# Sample values for generating a brownfield prompt pack.

REPO_PATH=/path/to/my-existing-app
DELTA_GOAL=Add export approval controls and audit-backed CSV export scope to the existing admin console without changing unrelated reporting behavior.
PRIMARY_REPO=/path/to/my-existing-app
RELATED_REPO_1=/path/to/shared-contracts
RELATED_REPO_2=/path/to/admin-identity-service
REQUESTED_DELTA=Introduce a contract-backed export approval surface, admin policy editor, and server-side enforcement for export scope.
UNTOUCHED_AREA_1=Existing dashboard read-only reporting pages
UNTOUCHED_AREA_2=Existing alert ingestion and notification workflows
IN_SCOPE_DELTA=admin export policy management, session capability contracts, and export enforcement
PLAN_OBJECTIVE=Add the smallest safe set of contracts, routes, UI pages, and tests needed to support admin-managed export scope.
